x5 数组开小了，runtime error，有时候也会显示wrong answer
x1 正难则反
x4 开long long（甚至爆long long）(for里面i 1e5*1e5) (函数返回值要开long long)
x1 多组数据初始化从0开始（某些板子，如带花树）
x6 边界条件 (如>=写成>，值为0 or max时..，0,0,0 or max,max,max时) 一个点or没有边的情况
x1 打错
x1 初始化时候注意范围
x1 predo里面写错
x1 读题
x1 while里面memset -> TLE
x2 没初始化:
1.没初始化全局变量的字符串

x2 t组数据，没清空

x2 特判

x正无穷 计算几何题本地有可能开不了那么多点，交上去的时候一定别忘了改N的值啊！！(我这只能开4e5)

x1 每次做匈牙利前要初始化vis数组

x1 答案%mod的题目 如果遇到a-b，那必写成(a-b+mod)%mod

x2 二分边界写错了，l=0,r=xxx写成l=1,r=xxx

x1 共线情况，重合情况，...

x1 除以0（包括mod）

做题最重要的是看清`本质`